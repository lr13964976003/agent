# Nodes that violate DAG requirements

- `experts_remaining_1`  
  Reason: has only outgoing edges (no incoming edge); every non-input node must have at least one incoming edge.

No other nodes violate the rules.